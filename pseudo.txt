/*An algorithm that asks a user to enter a positive number repeatedly
until the user enters a zero value, then determines and outputs the
largest of the numbers that were input.*/

Set PositiveNumber to Null
Set LargeNumber to Null

While PositiveNumber is greater 0
		Input a PositiveNumber
		When PositiveNumber is equal to 0
		Output a LargeNumber
End
		
		

		
/*
An algorithm that reads an arbitrary number of integers and then
returns their arithmetic average.
*/

Set Int to null
Set Count to null
Set Value to null

While Count is true
	Enter an Int
	
If Count stops

calculate the average Int arithmetic 
return the Value
end



/*
An algorithm that reads a grocery list and prints out the products
(in alphabetical order) that are still left to buy.
*/

Set an empty grocery list
Set Input to null
	
	if grocery list is true
		enter grocety list items
		
	else If grocery list input is done
	
	Print list and sort it alphabetically


//Flowchart question

Set X int is null
Set Y int is null
Set S int is null
Set A int is null
Set P int is null

get x, y
input x and y values
if s = x + y then A
if A = s/2 then B
if p= x * y then C

write S= A, A = B, P = C